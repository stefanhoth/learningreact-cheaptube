import VideoListItem from "./VideoListItem";

const VideoList = () => {
  return (
    <ul className="video-list">
      <VideoListItem />
      <VideoListItem />
      <VideoListItem />
    </ul>
  );
};

export default VideoList;
